# Web scraping (static HTML)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Offline TTS voice cloning (downloads XTTS v2 model ~2 GB on first run)
TTS>=0.22.0

# --- Playwright (only needed if the site requires JavaScript rendering) ---
# pip install playwright
# playwright install chromium
